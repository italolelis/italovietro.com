---
phase: 03-features-and-publishing
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - content/my-reading-list/index.en.md
  - content/my-reading-list/index.pt-br.md
  - config.toml
autonomous: false
requirements:
  - RATE-03
  - LANG-01
  - LANG-02

must_haves:
  truths:
    - "Every book entry in both language files has a rating parameter matching its tier placement"
    - "The page title is 'What I'm Reading' (EN) and 'O que estou lendo' (PT-BR)"
    - "The URL slug is /recommended-reading/ (EN) and /leituras-recomendadas/ (PT-BR)"
    - "Old URLs redirect to new URLs via Hugo aliases"
    - "Both language menu entries in config.toml point to the new URLs with new titles"
    - "The site builds and deploys without errors"
  artifacts:
    - path: "content/recommended-reading/index.en.md"
      provides: "English reading list with star ratings, new title/slug, redirect alias"
      contains: "rating="
    - path: "content/recommended-reading/index.pt-br.md"
      provides: "Portuguese reading list with star ratings, new title/slug, redirect alias"
      contains: "rating="
    - path: "config.toml"
      provides: "Updated menu entries for both languages"
      contains: "What I'm Reading"
  key_links:
    - from: "content/recommended-reading/index.en.md"
      to: "layouts/shortcodes/book.html"
      via: "shortcode invocation with rating param"
      pattern: 'rating="[0-9]"'
    - from: "content/recommended-reading/index.en.md"
      to: "config.toml"
      via: "slug matches menu url"
      pattern: "recommended-reading"
    - from: "content/recommended-reading/index.pt-br.md"
      to: "config.toml"
      via: "slug matches menu url"
      pattern: "leituras-recomendadas"
---

<objective>
Add star rating values to all book entries in both language files, rename the page (title, slug, directory), set up redirects from old URLs, update menu configuration, and verify the site builds cleanly for deployment.

Purpose: This is the final plan before v1 ships. It populates the star ratings (making the rendering from Plan 01 visible), rebrands the page to "What I'm Reading", and ensures both language versions are deployment-ready with working redirects.

Output: Both content files updated with ratings and new slugs/titles, config.toml updated with new menu entries, content directory renamed, Hugo build passing, old URLs redirecting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-features-and-publishing/03-CONTEXT.md
@.planning/phases/03-features-and-publishing/03-RESEARCH.md
@.planning/phases/03-features-and-publishing/03-UI-SPEC.md
@.planning/phases/03-features-and-publishing/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: book.html shortcode now accepts rating="N" parameter -->
<!-- Usage: {{< book title="..." author="..." link="..." type="book" rating="5" >}} -->
<!-- rating="5" renders 5 filled stars, rating="" or absent renders nothing -->

<!-- Rating-to-tier mapping (from UI-SPEC D-05): -->
<!-- Must Read = rating="5" -->
<!-- Recommended = rating="4" -->
<!-- Worth Your Time = rating="3" -->
<!-- Newsletters/Podcasts = rating="4" or rating="5" based on description tone -->
<!-- Currently Reading = no rating parameter (omit entirely) -->

<!-- Entry-by-entry rating values (from UI-SPEC Content Update Contract): -->
<!-- Clean Code: 5, Domain-Driven Design: 5, Grokking Algorithms: 5 -->
<!-- Refactoring: 4, Clean Architecture: 4, Site Reliability Engineering: 4, Staff Engineer: 4 -->
<!-- The Manager's Path: 5, An Elegant Puzzle: 5, The Phoenix Project: 5 -->
<!-- The Engineering Executive's Primer: 4 -->
<!-- Software Lead Weekly: 5, Level Up: 5, The Weekly Hagakure: 5, Changelog: 4 -->
<!-- The Critical Channel: 5, The Ladybug Podcast: 4, Kubernetes Podcast: 4, Go Time: 4 -->
<!-- Currently Reading placeholder: NO rating -->

<!-- Frontmatter changes (from UI-SPEC Page Rename Contract): -->
<!-- EN: title="What I'm Reading", slug="recommended-reading", aliases=["/my-reading-list/"] -->
<!-- PT-BR: title="O que estou lendo", slug="leituras-recomendadas", aliases=["/pt-br/minha-lista-de-leitura/"] -->

<!-- config.toml menu entries to update: -->
<!-- EN (line ~25-31): name="What I'm Reading", url="/recommended-reading/" -->
<!-- PT-BR (line ~88-94): name="O que estou lendo", url="/leituras-recomendadas/" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rating values to EN and PT-BR content, update frontmatter, rename directory, update config.toml</name>
  <files>
    content/my-reading-list/index.en.md
    content/my-reading-list/index.pt-br.md
    config.toml
  </files>
  <read_first>
    - content/my-reading-list/index.en.md
    - content/my-reading-list/index.pt-br.md
    - config.toml
    - .planning/phases/03-features-and-publishing/03-UI-SPEC.md
    - .planning/phases/03-features-and-publishing/03-CONTEXT.md
  </read_first>
  <action>
**Step 1: Update English content file (`content/my-reading-list/index.en.md`)**

Update the frontmatter fields (per D-10, D-11, D-12). Change `title:` to `What I'm Reading`. Change `slug:` to `recommended-reading`. Add an `aliases:` field with value `["/my-reading-list/"]`. Keep `socialImage` and `draft` unchanged.

Add `rating="N"` to every `{{< book` shortcode invocation. The rating parameter goes after `type="..."`. Do NOT add rating to the Currently Reading placeholder entry.

Specific rating values per entry (per D-05, UI-SPEC Content Update Contract):
- Currently Reading placeholder: NO rating (omit parameter entirely)
- Clean Code: `rating="5"`
- Domain-Driven Design: `rating="5"`
- Grokking Algorithms: `rating="5"`
- Refactoring: `rating="4"`
- Clean Architecture: `rating="4"`
- Site Reliability Engineering: `rating="4"`
- Staff Engineer: `rating="4"`
- The Manager's Path: `rating="5"`
- An Elegant Puzzle: `rating="5"`
- The Phoenix Project: `rating="5"`
- The Engineering Executive's Primer: `rating="4"`
- Software Lead Weekly: `rating="5"`
- Level Up: `rating="5"`
- The Weekly Hagakure: `rating="5"`
- Changelog: `rating="4"`
- The Critical Channel: `rating="5"`
- The Ladybug Podcast: `rating="4"`
- Kubernetes Podcast: `rating="4"`
- Go Time: `rating="4"`

Example transformation:
BEFORE: `{{< book title="Clean Code" author="Robert C. Martin" link="https://amzn.to/3f4tfO8" type="book" >}}`
AFTER:  `{{< book title="Clean Code" author="Robert C. Martin" link="https://amzn.to/3f4tfO8" type="book" rating="5" >}}`

**Step 2: Update Portuguese content file (`content/my-reading-list/index.pt-br.md`)**

Update the frontmatter fields (per D-13). Change `title:` to `O que estou lendo`. Change `slug:` to `leituras-recomendadas`. Add an `aliases:` field with value `["/pt-br/minha-lista-de-leitura/"]`. Keep `socialImage` and `draft` unchanged.

Add the SAME `rating="N"` values to every `{{< book` shortcode invocation as the English file. The entries are identical between languages -- same books, same ratings. Do NOT add rating to the Currently Reading placeholder.

**Step 3: Rename content directory**

After both content files are updated, rename the directory:
`mv content/my-reading-list content/recommended-reading`

The `slug` frontmatter in each file controls the URL, but renaming the directory keeps the codebase consistent (per RESEARCH.md recommended file change order).

**Step 4: Update config.toml menu entries**

Update the English menu entry (around line 28-29):
- Change `name = "My reading list"` to `name = "What I'm Reading"`
- Change `url = "/my-reading-list/"` to `url = "/recommended-reading/"`

Update the Portuguese menu entry (around line 91-92):
- Change `name = "Minha lista de livros"` to `name = "O que estou lendo"`
- Change `url = "/my-reading-list/"` to `url = "/leituras-recomendadas/"`

Do NOT change any other config.toml settings. Specifically, do NOT touch the `disableAliases = true` in the pagination sections (per Pitfall 2 in RESEARCH.md -- those are pagination-scoped, not global).
  </action>
  <verify>
    <automated>cd /Users/italovietro/projects/italovietro.com && echo "EN ratings:" && grep -c 'rating="' content/recommended-reading/index.en.md && echo "PT ratings:" && grep -c 'rating="' content/recommended-reading/index.pt-br.md && echo "EN frontmatter:" && head -7 content/recommended-reading/index.en.md && echo "PT frontmatter:" && head -7 content/recommended-reading/index.pt-br.md && echo "config menu:" && grep -A3 'identifier = "reading-list"' config.toml | head -8 && echo "Hugo build:" && hugo --gc --minify 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'rating="' content/recommended-reading/index.en.md` returns 19 (all entries except Currently Reading placeholder)
    - `grep -c 'rating="' content/recommended-reading/index.pt-br.md` returns 19
    - EN frontmatter contains `title: What I'm Reading`
    - EN frontmatter contains `slug: recommended-reading`
    - EN frontmatter contains `aliases:` with `/my-reading-list/`
    - PT-BR frontmatter contains `title: O que estou lendo`
    - PT-BR frontmatter contains `slug: leituras-recomendadas`
    - PT-BR frontmatter contains `aliases:` with `/pt-br/minha-lista-de-leitura/`
    - config.toml EN menu shows `name = "What I'm Reading"` and `url = "/recommended-reading/"`
    - config.toml PT-BR menu shows `name = "O que estou lendo"` and `url = "/leituras-recomendadas/"`
    - `content/my-reading-list/` directory no longer exists
    - `content/recommended-reading/` directory exists with both index files
    - Hugo build succeeds with 0 errors, 0 warnings
    - Currently Reading entry has NO `rating=` parameter in either language file
  </acceptance_criteria>
  <done>All 19 rated entries in both language files have correct star rating values. Page title, slug, and aliases are updated for both languages. Config.toml menu entries point to new URLs. Content directory renamed. Hugo builds cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of star ratings, anchor nav, and redirects</name>
  <what-built>
Complete Phase 3 implementation: star ratings on all book entries, restyled anchor navigation (plain text with dot separators), page renamed to "What I'm Reading", old URLs redirect to new URLs, both EN and PT-BR versions updated.
  </what-built>
  <how-to-verify>
1. Start Hugo dev server: `hugo server -D`
2. Visit `http://localhost:1313/recommended-reading/` and verify:
   a. Page title shows "What I'm Reading"
   b. Anchor nav at top shows "Currently Reading . Engineering Books . Management Books . Newsletters . Podcasts" as plain text links with dot separators (no pill buttons, no card background)
   c. Click each anchor link and verify it scrolls to the correct section
   d. Must Read books show 5 filled stars inline after the title (e.g., "Clean Code" followed by 5 star icons)
   e. Recommended books show 4 filled stars
   f. Newsletters show 4 or 5 stars per entry
   g. Currently Reading entry shows NO stars
   h. Stars are accent-colored (#2d96bd in light mode)
3. Toggle dark mode and verify:
   a. Stars change to dark accent color (#55bde2)
   b. Anchor nav links change to dark color
   c. Separator dots are visible in dark mode
4. Visit `http://localhost:1313/my-reading-list/` and verify it redirects to `/recommended-reading/`
5. Visit `http://localhost:1313/pt-br/leituras-recomendadas/` and verify:
   a. Portuguese version renders with same layout and star ratings
   b. Page title shows "O que estou lendo"
   c. Anchor nav links work for Portuguese section headings
6. Visit `http://localhost:1313/pt-br/minha-lista-de-leitura/` and verify it redirects to PT-BR new URL
7. Check site header nav bar:
   a. EN nav shows "What I'm Reading" linking to `/recommended-reading/`
   b. PT-BR nav shows "O que estou lendo" linking to `/leituras-recomendadas/`
8. Resize browser to mobile width (~375px):
   a. Anchor nav stacks vertically (no dot separators on mobile)
   b. Stars remain visible and properly sized
  </how-to-verify>
  <resume-signal>Type "approved" to confirm Phase 3 is complete, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `hugo --gc --minify` builds with 0 errors, 0 warnings
2. `grep -c 'rating="' content/recommended-reading/index.en.md` returns 19
3. `grep -c 'rating="' content/recommended-reading/index.pt-br.md` returns 19
4. `grep 'aliases' content/recommended-reading/index.en.md` shows `/my-reading-list/`
5. `grep 'aliases' content/recommended-reading/index.pt-br.md` shows `/pt-br/minha-lista-de-leitura/`
6. `grep 'What I.m Reading' config.toml` shows menu name updated
7. `grep 'O que estou lendo' config.toml` shows PT-BR menu name updated
8. `grep '/recommended-reading/' config.toml` shows EN menu URL updated
9. `grep '/leituras-recomendadas/' config.toml` shows PT-BR menu URL updated
10. `ls content/recommended-reading/` confirms directory exists with both language files
</verification>

<success_criteria>
- All 19 rated book entries in EN file have correct rating values (5 for Must Read, 4 for Recommended, etc.)
- All 19 rated book entries in PT-BR file have matching rating values
- Currently Reading placeholder has no rating in either file
- Page title is "What I'm Reading" (EN) and "O que estou lendo" (PT-BR)
- URL slugs are /recommended-reading/ (EN) and /leituras-recomendadas/ (PT-BR)
- Old URLs (/my-reading-list/ and /pt-br/minha-lista-de-leitura/) redirect via Hugo aliases
- config.toml menu entries updated for both languages
- Hugo build passes with 0 errors
- Visual checkpoint confirms stars render correctly in light and dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/03-features-and-publishing/03-02-SUMMARY.md`
</output>
